echo "# neogis-docker" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/nzi56/neogis-docker.git
git push -u origin main
----
git rm --cached ./postgres/db/neogis.backup
git rm --cached ./postgres/db -r